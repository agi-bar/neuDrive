# Copy to `neudrive.env` for local Docker Compose:
#   cp neudrive.env.example neudrive.env
#   docker compose --env-file neudrive.env up
#
# Copy to `neudrive.env` for local shell development, then export it:
#   cp neudrive.env.example neudrive.env
#   set -a; source neudrive.env; set +a
#   go run ./cmd/neudrive server
#
# Copy to `~/apps/neudrive/config/neudrive.env` for production deploy:
#   mkdir -p ~/apps/neudrive/config
#   cp neudrive.env.example ~/apps/neudrive/config/neudrive.env
#   ~/apps/neudrive/bin/deploy-main

# Shared PostgreSQL credentials.
POSTGRES_DB=neudrive
POSTGRES_USER=neudrive
POSTGRES_PASSWORD=change-this-postgres-password
POSTGRES_PORT=5432

# App runtime configuration.
PORT=8080
JWT_SECRET=change-this-to-a-random-secret
VAULT_MASTER_KEY=generate-with-openssl-rand-hex-32
# Per-user total storage quota in bytes. Set to 0 to disable the limit.
USER_STORAGE_QUOTA_BYTES=0
GITHUB_CLIENT_ID=your-github-oauth-app-client-id
GITHUB_CLIENT_SECRET=your-github-oauth-app-client-secret
POCKET_ID_PROVIDER_ID=pocket
POCKET_ID_ISSUER=https://pocket-id.example.com
POCKET_ID_DISCOVERY_URL=
POCKET_ID_CLIENT_ID=your-pocket-id-client-id
POCKET_ID_CLIENT_SECRET=your-pocket-id-client-secret
POCKET_ID_SCOPES=openid profile email
GITHUB_APP_CLIENT_ID=your-github-app-client-id
GITHUB_APP_CLIENT_SECRET=your-github-app-client-secret
GITHUB_APP_SLUG=your-github-app-slug

# Local defaults. For production, set these to your public domain.
PUBLIC_BASE_URL=http://localhost:8080
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Optional overrides:
# Pocket ID notes:
# - Set POCKET_ID_ISSUER for normal OIDC discovery.
# - Or leave POCKET_ID_ISSUER empty and set POCKET_ID_DISCOVERY_URL if discovery lives at a custom URL.
# DATABASE_URL=postgres://neudrive:change-this-postgres-password@localhost:5432/neudrive?sslmode=disable
# GIT_MIRROR_HOSTED_ROOT=/var/lib/neudrive/git-mirrors
# CLOUDFLARED_TUNNEL_TOKEN=
# NEUDRIVE_CAPTURE_OAUTH=1
# NEUDRIVE_CAPTURE_DIR=tmp/oauth-captures
