# `agenthub create`

Use this command when the user wants to create a first-class Agent Hub object.

## Current Public Form

- `agenthub create project <name>`

## Notes

- Keep the category after the verb.
- A leading `/` is optional if the user writes `/project`.
